function Team(name, ranking, country, players, achievements, lastMatches) {
    this.name = name,
    this.ranking = ranking,
    this.country = country,
    this.players = players,
    this.achievements = achievements,
    this.matches = lastMatches
}